import React from 'react';
import siteStyles from '../../../style/siteStyles';


function Bio() {
    const classes = siteStyles();
    return (
        <div className={classes.section_bio}>
            Bio
        </div>
    );
}

export default Bio;